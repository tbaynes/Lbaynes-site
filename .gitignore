# build output
dist/
.astro/

# dependencies
node_modules/

# lockfile — repo has historically built fresh on each deploy; do not pin
# until we have a deliberate reproducibility plan
package-lock.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# editor / local backups
*.astro-backup
